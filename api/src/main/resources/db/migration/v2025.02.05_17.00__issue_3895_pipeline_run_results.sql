CREATE TABLE pipeline.pipeline_run_result (
    run_id INTEGER REFERENCES pipeline.pipeline_run(run_id) NOT NULL,
    name VARCHAR NOT NULL,
    file_mask VARCHAR NOT NULL,
    path VARCHAR NOT NULL,
    CONSTRAINT pipeline_run_result_constrain UNIQUE(run_id, name, file_mask, path)
);

ALTER TABLE PIPELINE.DATASTORAGE_RULE ADD COLUMN IS_RESULT BOOLEAN DEFAULT FALSE NOT NULL;
ALTER TABLE PIPELINE.DATASTORAGE_RULE ADD COLUMN NAME TEXT DEFAULT NULL;
