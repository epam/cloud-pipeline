# Mount rules: Mount Storages located in the same cloud region with instance

Test verifies that storages are mounted only to the instances located in the same cloud region with the storage if option `Same region` is selected for *Object storages* and *File storages* in the Cloud region *Mount rules*.

**Prerequisites**:
- admin user
- Perform [3971_1](3971_1.md)

| Steps | Actions | Expected results |
| :---: |--- | --- |
| 1 | Open the **Settings** page | |
| 2 | Click the **CLOUD REGIONS** tab | |
| 3 | Select `region1` on the left panel | |
| 4 | In the **Mount rules:** section select <li> option `Same region` for ***Object storages*** <li> option `Same region` for ***File storages***. | |
| 5 | Click **Save** button | |
| 6 | Open the **Tools** page. Select the test tool | |
| 7 | At the tool page, hover over the **Run v** button | |
| 8 | Select the ***Custom settings*** option in the list | |
| 9 | Expand ***Exec environment*** section | |
| 10 | Select `region1` for **Cloud Region** | |
| 11 | Expand ***Advanced*** section | |
| 12 | Click on the text field in the **Limit mounts** section | |
| 13 | In an appeared ***Select data storages to limit mounts*** pop-up click the **Clear selection** button. | |
| 14 | Input `storage1` name into the *Search* field | `storage1`is shown in the available storages list |
| 15 | Select `storage1` | |
| 16 | Input `nfs_storage1` name into the *Search* field | `nfs_storage1` is shown in the available storages list |
| 17 | Select `storage1` | |
| 18 | Navigate to ***Exec environment*** section. Select `region2` for **Cloud Region** | `storage1` and `nfs_storage1` disappear from *Limit mounts* field |
| 19 | Click on the text field in the **Limit mounts** section | |
| 20 | Try to search `storage1` and `nfs_storage1` | `storage1` and `nfs_storage1` are missing in the available storages list |
