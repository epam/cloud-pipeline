name: Cloud-Pipeline-Dsitribution

concurrency: Cloud-Pipeline-Dsitribution

on:
  push:
    branches:
      - develop
      - 'release/**'
      - 'stage/**'
      - gha

  workflow_dispatch:

env:
  CLOUD_PIPELINE_BUILD_NUMBER_SEED: 13000

jobs:
  CLI_MacOS:
    runs-on: macos-10.15
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v2
        with:
          distribution: 'zulu'
          java-version: '8'
      - uses: actions/setup-python@v2
        with:
          python-version: '2.7.18'
          architecture: 'x64'
      - uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_DEFAULT_REGION }}
      - run: bash deploy/github_actions/gha_build_macos.sh
  Build_All:
    runs-on: ubuntu-18.04
    needs: CLI_MacOS
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v2
        with:
          distribution: 'zulu'
          java-version: '8'
      - uses: actions/setup-python@v2
        with:
          python-version: '2.7.18'
          architecture: 'x64'
      - uses: actions/setup-node@v2
        with:
          node-version: '10'
      - uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_DEFAULT_REGION }}
      - run: sudo -E bash deploy/github_actions/gha_pack_dist.sh
