apiVersion: apps/v1
kind: Deployment
metadata:
  name: cp-edge
  namespace: default
spec:
  selector:
    matchLabels:
      cloud-pipeline/cp-edge: "true"
      cloud-pipeline/role: "EDGE"
      cloud-pipeline/region: $CP_CLOUD_REGION_ID
  replicas: 1
  template:
    metadata:
      namespace: default
      labels:
        cloud-pipeline/cp-edge: "true"
        cloud-pipeline/role: "EDGE"
        cloud-pipeline/region: $CP_CLOUD_REGION_ID
        cloud-pipeline/core-component: "Deployment"
    spec:
      nodeSelector:
        cloud-pipeline/cp-edge: "true"
        cloud-pipeline/region: $CP_CLOUD_REGION_ID
      tolerations:
      - key: node-role.kubernetes.io/master
        effect: NoSchedule
      containers:
        - name: cp-edge
          image: ${CP_DOCKER_DIST_SRV}lifescience/cloud-pipeline:edge-$CP_VERSION
          imagePullPolicy: "Always"
          command: ["/init"]
          env:
          - name: NODE_OPTIONS
            value: "--max_old_space_size=4096"
          - name: CP_EDGE_REGION
            value: $CP_CLOUD_REGION_ID
          envFrom:
          - configMapRef:
              name: cp-config-global
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - /liveness.sh
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 4
          volumeMounts:
            - mountPath: /var/log
              name: edge-logs
            - mountPath: /opt/api/pki
              name: azurefile
              readOnly: true
            - mountPath: /opt/gitlab/pki
              name: gitlab-pki
              readOnly: true
            - mountPath: /opt/docker-registry/pki
              name: azurefile
              readOnly: true
            - mountPath: /opt/idp/pki
              name: azurefile
              readOnly: true
            - mountPath: /opt/edge/pki
              name: azurefile
              readOnly: true
            - mountPath: /opt/common/pki
              name: common-pki
              readOnly: true
            - mountPath: /etc/nginx/ext/external-apps
              name: edge-external-apps
              readOnly: true
            - mountPath: /opt/share-srv/pki
              name: share-pki
              readOnly: true
            - mountPath: /etc/nginx/ext/external-locs
              name: edge-external-locs
              readOnly: true
      dnsConfig:
        options:
          - name: ndots
            value: "1"
      volumes:
        - name: azurefile
          persistentVolumeClaim:
            claimName: azurefile
        - name: edge-logs
          hostPath:
            path: /opt/edge/logs/$CP_CLOUD_REGION_ID
        - name: gitlab-pki
          hostPath:
            path: /opt/gitlab/pki
        - name: common-pki
          hostPath:
            path: /opt/common/pki
        - name: edge-external-apps
          hostPath:
            path: /opt/edge/external-apps
        - name: share-pki
          hostPath:
            path: /opt/share-srv/pki
        - name:  edge-external-locs
          hostPath:
            path: /opt/edge/external-locs

