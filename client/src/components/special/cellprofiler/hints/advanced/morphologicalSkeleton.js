export default {
  'MorphologicalSkeleton': `
__MorphologicalSkeleton__ thins an image into a single-pixel wide skeleton.

Thinning is used to reduce each connected component in a binary image to a <em>single-pixel wide skeleton</em>. It is important to note that this is performed on binary images only.

Supports 2D? YES

Supports 3D? YES

Respects masks? NO`
};
