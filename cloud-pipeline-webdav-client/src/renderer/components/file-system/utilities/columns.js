const NAME = 'name';
const SIZE = 'size';
const CHANGED = 'changed';

const columns = [NAME, SIZE, CHANGED];
const sizes = {
  [SIZE]: 80,
  [CHANGED]: 150,
};

export {
  NAME,
  SIZE,
  CHANGED,
  columns,
  sizes,
};

export default columns;
