{
    "short_description": "Centos 7 with Xfce desktop environment (via noMachine) and CUDA toolkit",
    "instance_type": "p2.xlarge",
    "disk_size": "100",
    "default_command": "export CP_NM_PROXY_HOST=${CP_EDGE_EXTERNAL_HOST} && export CP_NM_PROXY_PORT=${CP_EDGE_CONNECT_EXTERNAL_PORT} && bash /etc/nomachine/nomachine_launcher.sh",
    "endpoints": [ "{ \"name\": \"NoMachine\", \"nginx\": { \"port\": \"8888\" }, \"isDefault\": false }" ]
}
