# Allow to specify Lustre FS metadata IOPS for the clusters scratch spac

Test verifies that the user can specify additional Lustre FS setup parameters:
-	CP_CAP_SHARE_FS__IOPS: File system metadata IO operations per second (IOPS) determine the number of files and directories that you can create, list, read, and delete per second.

**Prerequisites:**
-	Non-admin user

| Steps | Actions | Expected results |
| :---: |--- |--- |
| 1 | Login as the non-admin user from the prerequisites | |
| 2 | Open the **Tools** page | |	
| 3 | Select test tool | |	
| 4 | Choose Run -> Custom settings | |
| 5 | Expand **Exec environment** section | |
| 6 | Click **Configure cluster** | |
| 7 | Choose ***Cluster*** | | 
| 8 | Choose _Enable GridEngine_ | |
| 9 | In the *Configure File System* section select `LustreFS` value in the *Type* field | *Deployment type* field appears |
| 10 | In the *Deployment type* field select `PERSISTENT_2` value | *Throughput(MB/s/TiB)* and *IOPS(IOPS/TiB)* fields appear |
| 11 | Set `1200` value in the *Volume (GB)* field | |
| 12 | In the *Throughput(MB/s/TiB)* field select `500` value | |
| 13 | In the *IOPS(IOPS/TiB)* field select `1500` value | |
| 14 | Click **OK** button | |
| 15 | Expand ***Advanced*** section and change ***Price type*** to `On-demand` if needed | |
| 16 | Start a tool with a **Launch** button | |
| 17 | Click a newly started run entry in the ***ACTIVE RUNS*** menu | |
| 18 | Wait until task ***InitializeSharedFS*** appears and is marked as successfully completed | The task log contains: `Creating LustreFS with parameters: ?size=1200&type=PERSISTENT_2&throughput=500&iops=1500` |
| 19 | Click ***InitializeSharedFS*** task | |
| 20 | Wait until the run is fully initialized | Task ***InitializeEnvironment*** appears as successfully completed |

**After:**
-	Stop the run launched at step 16
