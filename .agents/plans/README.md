# Shared plans

Plans that outlived one branch, a directory per issue, branch or feature, each plan with its
`.ledger.md`. Everything else stays
in `.agents/plans.local/`, which is gitignored and per-developer.

The `plan-change` skill owns the rest — when a plan is worth a file, when it moves here, and the
ledger format.
