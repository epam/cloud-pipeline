<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">
    <bean class="com.epam.pipeline.billingreportagent.dao.PipelineRunDao" id="PipelineRunDao" autowire="byName">
        <property name="loadAllPipelineRunsQuery">
            <value>
                <![CDATA[
                    SELECT
                        run_id,
                        pipeline_id,
                        version,
                        start_date,
                        end_date,
                        parameters,
                        status,
                        terminating,
                        pod_id,
                        node_type,
                        node_disk,
                        node_ip,
                        node_id,
                        node_name,
                        node_image,
                        node_cloud_region,
                        docker_image,
                        cmd_template,
                        actual_cmd,
                        timeout,
                        owner,
                        service_url,
                        pod_ip,
                        commit_status,
                        last_change_commit_time,
                        config_name,
                        node_count,
                        parent_id,
                        entities_ids,
                        is_spot,
                        configuration_id,
                        pod_status,
                        prolonged_at_time,
                        last_notification_time,
                        last_idle_notification_time,
                        exec_preferences,
                        pretty_url,
                        price_per_hour,
                        state_reason,
                        non_pause,
                        node_real_disk,
                        node_cloud_provider
                    FROM
                        pipeline.pipeline_run
                    ORDER BY
                        start_date
                ]]>
            </value>
        </property>
    </bean>
</beans>
