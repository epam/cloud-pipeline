apiVersion: v1
kind: Service
metadata:
  labels:
    cloud-pipeline/cp-gitlab-db: "true"
  name: cp-gitlab-db
  namespace: default
spec:
  ports:
  - protocol: TCP
    port: ${CP_GITLAB_PSG_PORT}
    targetPort: ${PSG_PORT}
  selector:
    cloud-pipeline/cp-gitlab-db: "true"
