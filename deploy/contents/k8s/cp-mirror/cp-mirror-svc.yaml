apiVersion: v1
kind: Service
metadata:
  labels:
    cloud-pipeline/cp-mirror: "true"
  name: cp-mirror
  namespace: default
spec:
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    name: cp-mirror-port
  selector:
    cloud-pipeline/cp-mirror: "true"